#include <stdio.h>
#include <stdio.h>
#define MAX 10

typedef struct node {
    int child;
    struct node *next;
} *node;

typedef node Tree[MAX];

void initTree(){
    for (int i = 0; i < MAX; ++i){
        T[i] = NULL;
    }
}

void LeftMost_child(node n, Tree T){
   // function to find leftmost; 
}w

int main(){
    Tree T;

    initTree(T);


    return 0;
}